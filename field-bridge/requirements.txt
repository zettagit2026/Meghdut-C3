# CEMA cUAS field-bridge — Python worker deps
# Install with: pip install -r requirements.txt
#
# Shared across field-bridge/*.py. Versions match rf-bridge/requirements.txt
# for consistency across the two bridge directories.

requests==2.32.3
numpy>=1.26.0
scipy>=1.11.0  # gamutrf_infer.py (signal.spectrogram) and event_gating.py (find_peaks/peak_widths burst gating, 2026-07-24) — was already a de facto dependency of gamutrf_infer.py's spectrogram path; now pinned explicitly since event_gating.py depends on it directly
pyserial==3.5
pymavlink==2.4.41
websocket-client==1.8.0  # jam_bridge.py — WS client to backend/server.py's /api/ws/mavlink
Pillow>=10.0.0  # fpv_video_bridge.py — grayscale PNG reconstruction from AM envelope
scikit-learn>=1.4.0  # rf_features.py — spectral-feature classifier INFRASTRUCTURE (untrained, no shipped model; see rf_features.py docstring)
joblib>=1.3.0  # rf_features.py — model (de)serialization once a real trained model exists
canopen>=2.0  # canopen_parser.py — CANopen/CiA301 NMT decode INFRASTRUCTURE (HARDWARE-BLOCKED: needs a real CAN adapter, e.g. PEAK PCAN-USB or CANable; see canopen_parser.py docstring)
python-can==4.2.0  # canopen_parser.py + dronecan_parser.py — transport dependency of both canopen and dronecan (SocketCAN/pcan/gs_usb/slcan backends). Pinned exactly (not just >=4.0) because dronecan_parser.py's --self-test uses python-can's bustype="virtual" transport, and python-can>=4.6's VirtualBus.flush_tx_buffer() raises NotImplementedError, which crashes dronecan's bundled PythonCAN driver writer thread; 4.2.0's VirtualBus still implements it as a genuine no-op. See dronecan_parser.py docstring for full detail. Real hardware backends (socketcan/pcan/gs_usb) are unaffected by this pin either way.
dronecan==1.0.27  # dronecan_parser.py — DroneCAN/UAVCANv0 NodeStatus+Fix2 decode INFRASTRUCTURE (HARDWARE-BLOCKED: needs a real CAN adapter, same as canopen_parser.py; see dronecan_parser.py docstring). MIT license (DroneCAN Development Team / pydronecan project — matches the local ~/tool/pydronecan clone's version.py and LICENSE exactly). Pinned to the exact version installed/tested for task #44 (confirmed via pip freeze in the /private/tmp/dctest venv used for that task's --self-test run).
# spectrogram_similarity_bridge.py — Classidyne-style spectrogram k-NN similarity
# classifier INFRASTRUCTURE (DATA-BLOCKED: no real, multi-class reference
# library exists yet; see spectrogram_similarity_bridge.py docstring). Not
# wired into any live pipeline. torch/torchvision already required by
# gamutrf_infer.py; timm/chromadb are new for this module only.
timm>=1.0.0
chromadb>=0.5.0
# thermal_radiometric.py (task #64/#83 close-out, FLIR R-JPEG per-pixel
# radiometric temperature extraction via Planck's law) — NO new pip
# dependency: numpy/Pillow are already required above. Requires the
# separate `exiftool` CLI on PATH (e.g. `brew install exiftool` /
# `apt install libimage-exiftool-perl`) to read FLIR's proprietary binary
# calibration-constant + raw-thermal-plane metadata segment; this is an
# external system tool invocation (subprocess), not a linked code
# dependency, and is NOT installed on this Mac dev copy (verified via
# `which exiftool`) — see thermal_radiometric.py docstring.
# iff_crypto.py / iff_beacon_bridge.py (task #60, IFF LoRa beacon w/
# environmental-entropy key generation) — deliberately add NO new dependency
# here: the whole crypto construction (HKDF-SHA256, HMAC-SHA256) is built
# directly on Python's stdlib hmac/hashlib, already a transitive dependency
# of every bridge that uses requests. A LoRa hardware driver dependency
# (e.g. pySX127x, or just pyserial if an ESP32/RFM95 front-end is used) will
# need to be added HERE once real LoRa RX hardware exists — see
# iff_beacon_bridge.py's HARDWARE STATUS docstring section.
