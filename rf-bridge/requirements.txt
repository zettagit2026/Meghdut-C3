# CEMA cUAS RF Bridge — Python worker deps (MAVLink serial bridge only;
# task #36 removed the HackRF scanner from this directory, along with its
# numpy dependency — see field-bridge/requirements.txt for that pipeline).
# Install with: pip install -r requirements.txt

pyserial==3.5
pymavlink==2.4.41
requests==2.32.3
websocket-client==1.8.0
python-dotenv==1.0.1
